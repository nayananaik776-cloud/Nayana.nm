#include <stdio.h>
#include <string.h>
int main()
{
    char source[100];
    char dest[100];
    printf("Enter the string : ");
    scanf("%s", source);
    strcpy(dest, source);
    printf("source string : %s\n", source);
    printf("destination string : %s\n", dest);
     return 0;
}
